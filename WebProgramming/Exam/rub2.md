1. Какие пункты из предложенных однозначно являются недостатками концепции RIA?
Правильные ответы:

Высокий уровень абстракции
Фреймворки, реализующие концепцию, большие, включают в себя много полезных функций, но сложно устроены

2. Какие аннотации из предложенных могут быть использованы для того, чтобы "попросить" контейнер внедрить зависимость в CDI bean?
Правильные ответы:

@Inject

(Только @Inject — стандартная аннотация CDI для внедрения зависимостей. Остальные не используются для этой цели в CDI.)
3. Что из перечисленного является платформами Java?
Правильный ответ:

JavaSE
JavaEE
JavaCard

(JavaTV не является официальной платформой Java.)
1. В каких из ниже перечисленных ситуаций можно применить валідатор (validator) в JSF?
Правильные ответы:

Отобразить на странице стоимость заказа с использованием тысячных разделителей (пробелов) в числе
Разрешить ввод номера телефона, начиная только с +7
Реализовать обязательное для заполнения поле "личный номер ИСУ" в форме по отчислению студента
Разрешить ввод любого положительного числа, но проверить, что введенное число находится в диапазоне от 120 до 210

(Все перечисленные случаи — типичные задачи валидации данных на форме в JSF.)

2. EJB бины в отличие от CDI бинов (выберите корректные утверждения)
Правильные ответы:

Не могут быть использованы в JSF facelets
Предоставляют больше возможностей, например, удаленный вызов бинов
Предоставляют больше возможностей, например, возможность создания экземпляра бина посредством фабрики бинов

4. Напишите название ключевого интерфейса из спецификации JPA, благодаря которому можно сохранять, изменять, удалять и извлекать объекты из базы данных.
Ответ:
EntityManager
5. Напишите аннотацию из спецификации JAX-RS, которая определяет тип тела запроса.
Ответ:
@Consumes
5. Напишите аннотацию из спецификации JAX-RS, которую нужно использовать для получения параметров из заголовков запроса.
Ответ:
@HeaderParam
6. Для каких задач можно использовать JAX-RS?
Правильный ответ:

Реализация веб-приложений с раздельными фронтенд и бэкенд приложениями, взаимодействующими друг с другом по REST API

7. Какие преимущества можно выделить у принципа рендеринга SSR?
Правильные ответы:

Уменьшения объема трафика, который необходим для взаимодействия с веб-сайтом, по сравнению с CSR, так как по сети после первоначальной загрузки передаются только данные, связанные с динамическими операциями, в формате JSON
Приложения, разработанные на основе этого принципа более безопасны, так как информация о видимых пользователем чувствительных данных не передается по сети

8. Что из ниже перечисленного о React верно?
Правильные ответы:

1,2,3,4

9. Что из ниже перечисленного о Angular верно?
Правильный ответ:

В Angular, в отличие от React, существует двунаправленная связь между компонентами, которая позволяет связать состояние дочернего и родительского компонентов

10. Какой из ниже перечисленных инструментов используется в данном примере? (код с createSlice, actions, reducer)
Правильный ответ:
Redux Toolkit
10. Какой из ниже перечисленных инструментов используется в данном примере? (код с useAtomValue)
Правильный ответ:
Jotai
11. Привести пример Java класса, использующего передаваемый через конструктор экземпляр JPA EntityManager, для реализации следующих операций со студентами: создание (сохранение), редактирование персональной информации, отчисление (удаление), получение записи о студенте по идентификатору.
(Открытый вопрос — пример кода)
Пример правильного ответа (Repository-класс):

```java
import jakarta.persistence.EntityManager;

public class StudentRepository {
    private final EntityManager em;

    public StudentRepository(EntityManager em) {
        this.em = em;
    }

    public void create(Student student) {
        em.persist(student);
    }

    public void updatePersonalInfo(Student student) {
        em.merge(student);
    }

    public void delete(Student student) {
        em.remove(student);
    }

    public Student findById(int id) {
        return em.find(Student.class, id);
    }
}
```

Дополнительные вопросы из других частей
3. Согласно какому принципу не имеет значения, где физически расположен вызываемый код...
Правильные ответы:

Location Transparency
Java Naming & Directory Interface

3. Что из ниже перечисленного являются корректными утверждениями о принципе DI (Dependency Injection)?
Правильные ответы:

Внедрение зависимостей позволяет переложить ответственность за инициализацию объектов на сторонний инструмент
Внедрение зависимостей можно реализовать в любом приложении на платформе Java SE без использования сторонних библиотек

6. Какие из приведенных ниже утверждений о спецификации JAX-RS верны?
Правильный ответ:

JAX-RS позволяет реализовать API бэкенда приложения без написания HTTP ответов вручную разработчиков

7. Данный принцип рендеринга подразумевает... Напишите аббревиатуру данного принципа.
Ответ:
SSR (Server-Side Rendering)

-----------------------------------------------------------------------------------------------
1.1
В каких из ниже перечисленных ситуаций можно применять конвертер (converter) в JSF?

1. Отобразить на странице стоимость заказа с использованием тысячных разделителей (пробелов) в числе
2. Разрешить ввод номера телефона, начиная только с +7
3. Реализовать обязательное для заполнения поле “личный номер ИСУ” в форме по отчислению студента
4. Разрешить ввод любого положительного числа, но проверить, что введенное число находится в диапазоне от 120 до 210

Ответ: 1

1.2
Для чего можно использовать перехватчики (interceptors) жизненного цикла CDI бинов?

1. Логирование перед и после вызова методов CDI бинов
2. Открытие соединение к базе данных после успешного создания экземпляра CDI бина
3. Проверка прав доступа пользователя для вызова метода CDI бина от имени пользователя
4. Валидация аргументов вызываемого метода CDI бина

Ответ: 1,3,4

1.3
Данный принцип позволяет не создавать требуемые зависимости в конструкторе объекта, а получить их “извне” уже проинициализированными. При этом может использоваться как сторонний инструмент, такой как Angular или контейнер из Jakarta EE, так и самописный код, поставляющие зависимости через простую передачу параметров в конструктор. Также согласно данному принципу можно определить области видимости зависимостей, например, “всё приложение” или “один запрос”. Напишите аббревиатуру принципа, о котором идет речь

Ответ: DI

1.4
Напишите аннотацию из спецификации JPA, которую необходимо использовать, чтобы настроить правило генерации идентификатора сущности. Ответ должен начинаться с символа @
Ответ: @GeneratedValue

1.5
Напишите аннотацию из спецификации JAX-RS, которую нужно использовать для получения параметров из “пути” запроса. Ответ должен начинаться с символа @
Ответ: @PathParam

1.6. Какие из приведенных ниже утверждений о спецификации JAX-RS верны?

1. JAX-RS входит в Java EE Full Profile
2. JAX-RS позволяет разработчику реализовать REST API
3. Благодаря RMI можно использовать удаленный вызов JAX-RS ресурсов, чтобы использовать более эффективные бинарные форматы передачи данных вместо текстовых, используемых в HTTP
4. JAX-RS, как и Spring, поддерживает тесную интеграцию с шаблонизатором Thymeleaf благодаря проекту Jakarta EE Easy Boot

Ответ: 1,2

1.7 Какие преимущества можно выделить у принципа рендеринга CSR?

1. Уменьшения объема трафика, который необходим для взаимодействия с веб-сайтом, по сравнению с SSR, так как по сети после первоначальной загрузки передаются только
данные, связанные с динамическими операциями, в формате JSON
2. Проблема с поисковой индексацией в силу того, что поисковые роботы не исполняют JavaScript
3. Сравнительная “простота” реализации приложений на базе этого принципа, в силу того, что современные фреймворки широкого его поддерживают, и в силу того, что построение
приложений на подобных фреймворках “простое” с точки зрения технической реализации
4. Применение этого принципа снижает нагрузку на браузер клиента, так как всю работу по рендерингу страниц берет на себя сервер

Ответ: 1,3

1.8. Что из ниже перечисленного о React верно?

1. Чтобы реализовать двунаправленную связь между компонентами в React, можно использовать хук useTwoWayBinding, передав ему в качестве параметра props компонента
2. Для того чтобы определить props для компонента необходимо использовать специальный декоратор @Input, пометив им свойство классового компонента
3. Для того чтобы React мог корректно обновлять узлы в виртуальном дереве компонентов, необходимо компоненты, добавляемые в JSX разметку в цикле, снабжать специальным уникальным атрибутом key
4. Для управления глобальным состоянием компонента в React можно использовать хук useState

Ответ: 3

1.9. Что из ниже перечисленного о Angular верно?

1. Для отображения списка элементов в шаблоне компонента можно использовать блок управления потоком исполнения @for
2. Angular позволяет разработчику определить произвольный код, который должен выполняться на определенных фазах жизненного цикла компонента
3. Компонент в Angular является подвидом более общего понятия - директивы
4. Для управления глобальным состоянием компонентов в Angular можно использовать только специальную библиотеку Redux

Ответ 1,2,3

1.10 Какой из ниже перечисленных инструментов используется в данном примере?

export const chapterService = gatewayApi.injectEndpoints({
    endpoints: (builder) => ({
        getChapterList: builder.query<Array<Chapter>, GetParams>({
            query: ({ parentId, url: `${BASE_URL}` }) => ({ url }),
            providesTags: ['Chapters'],
        }),
        createChapter: builder.mutation<Chapter, CreateParams>({
            query: ({ parentId, ...body }) => ({
                url: `${BASE_URL}/${parentId}/chapters`,
                method: 'POST',
                body,
            }),
            invalidatesTags: ['Chapters'],
        }),
    }),
});

1. Redux
2. Redux Toolkit
3. Jotai
4. Redux Toolkit Query

Ответ: 4

1.11 Интерфейс на React, реализующий калькулятор с базовыми операциями над двумя дробными числами: сложение, вычитание, умножение, деление. При попытке деления на ноль должен выскакивать alert “Вы были отчислены”.
``` Java
import React, { useState } from 'react';

const Calculator = () => {
  const [firstNumber, setFirstNumber] = useState(0);
  const [secondNumber, setSecondNumber] = useState(0);
  const [result, setResult] = useState(null);
  const [error, setError] = useState(null);

  const handleOperation = (operation) => {
    const num1 = parseFloat(firstNumber);
    const num2 = parseFloat(secondNumber);

    if (operation === '/' && num2 === 0) {
      setError('Вы были отчислены');
      setResult(null);
      return;
    }

    setError(null);

    let res;
    switch (operation) {
      case '+':
        res = num1 + num2;
        break;
      case '-':
        res = num1 - num2;
        break;
      case '*':
        res = num1 * num2;
        break;
      case '/':
        res = num1 / num2;
        break;
      default:
        res = 0;
    }

    setResult(res);
  };

  return (
    <div>
      <input
        type="number"
        value={firstNumber}
        onChange={(e) => setFirstNumber(e.target.value)}
        placeholder="Первое число"
      />
      <input
        type="number"
        value={secondNumber}
        onChange={(e) => setSecondNumber(e.target.value)}
        placeholder="Второе число"
      />
      <button onClick={() => handleOperation('+')}>+</button>
      <button onClick={() => handleOperation('-')}>-</button>
      <button onClick={() => handleOperation('*')}>*</button>
      <button onClick={() => handleOperation('/')}>/</button>
      {error && <p style={{ color: 'red' }}>{error}</p>}
      {result !== null && !error && <p>Результат: {result}</p>}
    </div>
  );
};

export default Calculator;
```

2.1. Что из перечисленного является преимуществом JSF?

1. Фреймаорк реализует шаблон MVP, что позволяет разделять бизнес-логику и логику отображения интерфейса
2. Можно реализовать и баканд, и фронтенд одновременно в рамках одного приложения
3. Существуют библиотеки с готовыми компонентами, которые позволяют существенно ускорить разработку веб-приложений
4. Высокий уровень абстракции

Ответ: 2,3,4

2.2 Какие аннотации из предложенных могут быть использованы для того, чтобы обозначить область видимости CDI бина?

1. @CustomScoped
2. @NoneScoped
3. @Dependent
4. @ConversationScoped

Ответ: 3,4

2.3. Что из ниже перечисленного являются корректными утверждениями о принципе DI (Dependency Injection)?

1. Внедрение зависимостей возможно только при использовании сторонних библиотек, разработчик не может реализовать данный принцип сам  
2. Внедрение зависимостей является реализацией принципа IoC (Inversion of Control)  
3. Внедрение зависимостей позволяет переложить ответственность за инициализацию объектов на сторонний инструмент  
4. Внедрение зависимостей можно реализовать в любом приложении на платформе Java SE без использования сторонних библиотек  

Ответ: 2,3,4

2.4. Напишите аннотацию из спецификации JPA, которую необходимо использовать, чтобы пометить бин как сущность, подлежащую хранению в базе данных. Ответ должен начинаться с символа @  
Ответ: @Entity

2.5. Напишите аннотацию из спецификации JAX-RS, которая определяет URL, к которому привязывается JAX-RS ресурс. Ответ должен начинаться с символа @  
Ответ: @Path

2.6. Какие из приведенных ниже утверждений о спецификации JAX-RS верны?  

1. JAX-RS обязывает разработчика использовать подход REST к построению API  
2. JAX-RS не поддерживает работу с файлами, для работы с ними необходимо реализовывать отдельный сервис  
3. JAX-RS позволяет реализовать API байкера, приложения без написания HTTP ответов вручную разработчиков  
4. В большинстве реализаций спецификации JAX-RS по умолчанию используется XML в качестве формата тела запроса и тела ответа

Ответ: 3,4

7. Данный принцип рендеринга подразумевает, что с сервера приходит на клиент зачастую "пустая" HTML страница, а её содержимое вычисляется браузером клиента динамически благодаря возможностям JavaScript. Напишите аббревиатуру данного принципа.

Ответ: CSR

2.8. Что из ниже перечисленного о React верно?  

1. Для управления локальным состоянием в React используется хук useState  
2. При использовании Strict Mode все хуки useEffect применяются дважды для проверки корректности сайда-эффектов  
3. Функция, определяющая компонент, вызывается на каждый рендер компонента  
4. React использует виртуальный DOM для оптимизации отрисовок и обновления только тех узлов в оригинальной DOM модели страницы, что действительно изменилось  

Ответ: 1,2,3,4

-------------------------------------
1. Что из ниже перечисленного происходит на фазе обработки событий Update model values в JSF?

Вызов обработчика событий, определенного в Managed bean
Генерация ответа сервера в результате возникновения ошибки при вызове метода бина

1. Что из ниже перечисленного происходит на фазе обработки событий Invoke application в JSF?

Вызов обработчика событий, определенного в Managed bean
Генерация ответа сервера в результате возникновения ошибки при вызове метода бина

2. Какие из ниже перечисленных аннотаций используются для определения перехватчиков (interceptors) жизненного цикла CDI бинов?

@PostConstruct
@AroundInvocation

(@Init не является стандартной аннотацией CDI; @AroundExec не существует.)
2. Для чего может быть использована фабрика CDI бинов (аннотация @Produces)?

"Превращать" экземпляр объекта класса из некоторой стороны библиотеки в управляемый контейнером CDI бин

3. Что из ниже перечисленного являются платформами Java?

JavaSE
JavaEE
JavaCard

(JavaTV не является официальной платформой Java.)
3. В чем различие между Java EE и Jakarta EE?

Jakarta EE, в отличие от Java EE, является открытым стандартом и поддерживается сообществом
В Java EE была добавлена спецификация JAX-RS для построения RESTful Web Services, и была убрана устarevшая спецификация JSF

3. Укажите название профиля платформы Java EE, отличающегося легковесностью. Application сервер, реализующий этот профиль, поддерживает все наиболее используемые спецификации Java EE.

Web

3. Согласно какому принципу не имеет значения, где физически расположен вызываемый код или запрашиваемый ресурс. Разработчик просто "запрашивает" у инструмента выполнения доступ к самому инструменту инкапсулирует в себе логику вызова/обработки запроса. Выберите принцип(-ы), который имеет речь.

Location Transparency
Java Naming & Directory Interface

4. Данная спецификация Java EE может быть использована для получения экземпляра EntityManager, управляемого контейнером (Container Managed). Напишите аббревиатуру. Подсказка: это НЕ JPA.

@PersistenceContext

4. Напишите название ключевого интерфейса из спецификации JPA, благодаря которому можно сохранять, изменять, удалять и извлекать объекты из базы данных.

EntityManager

5. Напишите аннотацию из спецификации JAX-RS, которую нужно использовать для получения параметров из заголовков запроса. Ответ должен начинаться с символа @.

@HeaderParam

5. Напишите аннотацию из спецификации JAX-RS, которую нужно использовать для получения параметров из заголовков запроса.

@HeaderParam

5. Напишите аннотацию из спецификации JAX-RS, которая определяет тип тела запроса. Ответ должен начинаться с символа @.

@Consumes

5. Напишите аннотацию из спецификации JAX-RS, которая определяет URL, к которому привязывается JAX-RS ресурс. Ответ должен начинаться с символа @.

@Path

5. Напишите аннотацию из спецификации JAX-RS, которую нужно использовать для получения параметров из заголовков запроса. Ответ должен начинаться с символа @.

@HeaderParam

6. Для каких задач можно использовать JAX-RS?

Реализация веб-приложений с раздельными фронтенд и бэкенд приложениями, взаимодействующими друг с другом по REST API

6. Какие из приведенных ниже утверждений о спецификации JAX-RS верны?

JAX-RS позволяет разработчику использовать подход REST к построению API
JAX-RS не поддерживает работу с файлами, для работы с ними необходимо реализовать отдельный сервлет

7. Данный принцип рендеринга подразумевает, что с сервера приходит на клиент заготовку "пустой" HTML страницы, a её содержимое вычисляется браузером клиента динамически благодаря возможности JavaScript. Напишите аббревиатуру данного принципа.

CSR

7. Какие преимущества можно выделить у принципа рендеринга SSR?

Уменьшения объема трафика, который необходим для взаимодействия с веб-сайтом, по сравнению с CSR, так как по сети после первоначальной загрузки передаются только данные, связанные с динамическими операциями, в формате JSON
Приложения, разработанные на основе этого принципа более безопасны, так как информация о видимых пользователем чувствительных данных не передается по сети

7. Что из ниже перечисленного может задавать в package.json файле?

Название пакета

8. Что из ниже перечисленного о React верно?

React использует виртуальный DOM для оптимизации отрисовок и обновления только тех узлов в оригинальной DOM модели страницы, что действительно изменилось

9. Что из ниже перечисленного о Angular верно?

В Angular существует специальный вид связи между компонентами, называемый трехуровневым, который позволяет связывать не только родительский и дочерний компоненты, но также и atom глобального состояния

10. Какой из ниже перечисленных инструментов используется в данном примере? (код с useAtomValue)

Jotai

10. Какой из ниже перечисленных инструментов используется в данном примере? (код с createSlice)

Redux Toolkit

10. Какой из ниже перечисленных инструментов используется в данном примере? (код с gatewayApi.injectEndpoints)

Redux Toolkit Query

------------------------------------------------------------------------
1. Что из ниже перечисленного происходит на фазе обработки событий Invoke application в JSF?
Варианты:

Установка ранее проверенных значений в свойства нужного CDI bean
Вызов обработчика события, определенного в Managed bean (правильный)
Выполнение логики проверки значений, написанной внутри метода Managed bean
Генерация ответа сервера в результате возникновения ошибки при вызове метода бина (правильный)

Правильные варианты: Вызов обработчика события, определенного в Managed bean; Генерация ответа сервера в результате возникновения ошибки при вызове метода бина.
2. Для чего может быть использована фабрика CDI бинов (аннотация @Produces)?
Варианты:

Создавать перехватчики жизненного цикла создаваемого бина
Изменять имя бина динамически после его фактического создания
Создавать бин, класс которого не имеет конструктора без параметров
"Превращать" экземпляр объекта класса из некоторой стороны библиотеки в управляемый контейнером CDI бин (правильный)

Правильный вариант: "Превращать" экземпляр объекта класса из некоторой стороны библиотеки в управляемый контейнером CDI бин.
2. Какие из ниже перечисленных аннотаций используются для определения перехватчиков (interceptors) жизненного цикла CDI бинов?
Варианты:

@PostConstruct (правильный)
@AroundInvocation (правильный)
@AroundExec
@Init

Правильные варианты: @PostConstruct; @AroundInvocation.
3. Что из ниже перечисленного являются правильными утверждениями о принципе DI (Dependency Injection)?
Варианты:

Внедрение зависимостей возможно только при использовании сторонних библиотек, разработчик не может реализовать данный принцип сам (неправильный)
Внедрение зависимостей является реализацией принципа IoC (Inversion of Control) (правильный)
Внедрение зависимостей позволяет переложить ответственность за инициализацию объектов на сторонний инструмент (правильный)
Внедрение зависимостей можно реализовать в любом приложении на платформе Java SE без использования сторонних библиотек (неправильный)

Правильные варианты: Внедрение зависимостей является реализацией принципа IoC (Inversion of Control); Внедрение зависимостей позволяет переложить ответственность за инициализацию объектов на сторонний инструмент.
3. Укажите название профиля платформы Java EE, отличающегося легковесностью. Application сервер, реализующий этот профиль, поддерживает все наиболее используемые спецификации Java EE.
Открытый вопрос. Правильный ответ: Web.
4. Напишите аннотацию из спецификации JPA, которую необходимо использовать, чтобы пометить бин как сервис, предоставляющий индексированное хранение в базе данных. Ответ должен начинаться с символа @.
Открытый вопрос. Правильный ответ: @PersistenceContext.
5. Напишите аннотацию из спецификации JAX-RS, которую нужно использовать для получения параметров из заголовков запроса. Ответ должен начинаться с символа @.
Открытый вопрос. Правильный ответ: @HeaderParam.
5. Напишите аннотацию из спецификации JAX-RS, которую нужно использовать для задания "глобального" пути, к которому будут привязаны все ресурсы. Ответ должен начинаться с символа @.
Открытый вопрос. Правильный ответ: @ApplicationPath.
5. Напишите метод из спецификации JAX-RS, который нужно использовать для получения параметров из заголовков запроса. Ответ должен начинаться с символа @.
Открытый вопрос. Правильный ответ: @HeaderParam (это аннотация, но вопрос, кажется, на аннотацию).
6. Какие из приведенных ниже утверждений о спецификации JAX-RS верны?
Варианты:

JAX-RS входит в Java EE Full Profile (правильный)
JAX-RS позволяет разработчику реализовать REST API (правильный)
Благодаря RMI можно использовать удаленный вызов JAX-RS ресурсов, чтобы использовать более эффективные бинарные форматы передачи данных вместо текстовых, используемых в HTTP
JAX-RS, как и Spring, поддерживает технику интеграции с шаблонизатором Thymeleaf благодаря проекту Jakarta EE Easy Boot (неправильный)

Правильные варианты: JAX-RS входит в Java EE Full Profile; JAX-RS позволяет разработчику реализовать REST API.
6. Для каких задач можно использовать JAX-RS?
Варианты:

Реализация веб-приложений на базе концепции RIA
Реализация веб-приложений на основе базового JAX-RS Template Processing Engine
Реализация веб-приложений с разделенным фронтендом и бэкендом, взаимодействующими друг с другом по REST API (правильный)
Реализация веб-приложений на основе базового MVP, с разделенными бизнес-логикой и логикой интерфейса (правильный)

Правильные варианты: Реализация веб-приложений с разделенным фронтендом и бэкендом, взаимодействующими друг с другом по REST API; Реализация веб-приложений на основе базового MVP, с разделенными бизнес-логикой и логикой интерфейса.
7. Что из ниже перечисленного можно задавать в package.json файле?
Варианты:

Название пакета (правильный)
Список зависимостей пакета
Версия пакета
Скрипты, которые можно выполнять для данного пакета

Правильный вариант: Название пакета.
1. Данный принцип рендеринга подразумевает, что с сервера приходит на клиент заготовку "пустой" HTML страницы, a её содержимое вычисляется браузером клиента динамически благодаря возможности JavaScript. Напишите аббревиатуру данного принципа.
Открытый вопрос. Правильный ответ: CSR.
1. Какие преимущества можно выделить у принципа рендеринга SSR?
Варианты:

Применение этого принципа позволяет команде разработчиков и бизнесу добавлять больше средств в аренду серверных мощностей
Уменьшения объема трафика, который необходим для взаимодействия с веб-сайтом, по сравнению с CSR, так как по сети после первоначальной загрузки передаются только данные, связанные с динамическими операциями, в формате JSON (неправильный, это для CSR)
Динамичный принцип позволяет логике меняться во время до первой отрисовки страницы в сравнении с CSR за счет того, что основная нагрузка, связанная с рендерингом, ложится на сервер, a не на клиент
Приложения, разработанные на основе этого принципа более безопасны, так как информация о видимых пользователем чувствительных данных не передается по сети (правильный)

Правильный вариант: Приложения, разработанные на основе этого принципа более безопасны, так как информация о видимых пользователем чувствительных данных не передается по сети.
8. Что из ниже перечисленного о React верно?
Варианты:

Компоненты в React бывают только функциональные, классовых компонентов не существует
При использовании Strict Mode все хуки useEffect применяются дважды для проверки корректности сайд-эффектов (правильный)
Существует возможность реализации виртуального роутера в React приложениях - React Browser Router
React поддерживает как одностраничную, так и двухнаправленную связь между компонентами

Правильный вариант: При использовании Strict Mode все хуки useEffect применяются дважды для проверки корректности сайд-эффектов.
8. Что из ниже перечисленного о React верно?
Варианты:

Для управления глобальным состоянием в React можно использовать только библиотеку Redux
Функция компонента вызывается каждый раз, когда React рендерит компонент (правильный, но в изображении не)
В React props-ы компонентов неизменяемы, поэтому их изменение ни к чему не приводит
React можно использовать не только XMLHttpRequest, но и Fetch API (правильный)

Правильный вариант: React можно использовать не только XMLHttpRequest, но и Fetch API.
9. Что из ниже перечисленного о Angular верно?
Варианты:

В Angular существует способ определения свойств компонента, принимающих значения от родителя, как при помощи декоратора, так и при помощи синтаксиса
В Angular существует как функциональные, так и классовые компоненты, хотя последние синтаксически устаревшими и не рекомендуются к использованию
В Angular нет встроенных механизмов роутинга, необходимо подключать дополнительную библиотеку для реализации маршрутизации
В Angular можно использовать хук useEffect для того, чтобы определить сайд-эффект (правильный - нет, useEffect - React)

Правильный вариант: В Angular существует служебные конфигурационные файлы, названия которых не стоит изменять, чтобы не сломать приложение.
10. Какой из ниже перечисленного инструментов используется в данном примере? (код с useAtomValue, createSlice, injectEndpoints)
Варианты:

Redux
Redux Toolkit (для createSlice)
Jotai (для useAtomValue)
Redux Toolkit Query (для injectEndpoints)

Правильный вариант: Зависит от кода: для useAtomValue - Jotai; для createSlice - Redux Toolkit; для injectEndpoints - Redux Toolkit Query.
11. Задание: Реализовать интерфейс на JSF для отображения ленты новостей. Каждая новость должна быть снабжена названием, описанием, изображением и датой. Должна быть возможность создания новой новости. Список новостей должен являться динамическим и браться из CDI бина.
  1. 实体
```java
import jakarta.enterprise.context.ApplicationScoped;
import java.io.Serializable;
import java.time.LocalDate;

@ApplicationScoped
public class News implements Serializable {
    private Long id;
    private String title;
    private String description;
    private String imageUrl; // URL изображения
    private LocalDate date;

    // Конструкторы
    public News() {}

    public News(String title, String description, String imageUrl) {
        this.title = title;
        this.description = description;
        this.imageUrl = imageUrl;
        this.date = LocalDate.now();
    }

    // Геттеры и сеттеры
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }

    public String getTitle() { return title; }
    public void setTitle(String title) { this.title = title; }

    public String getDescription() { return description; }
    public void setDescription(String description) { this.description = description; }

    public String getImageUrl() { return imageUrl; }
    public void setImageUrl(String imageUrl) { this.imageUrl = imageUrl; }

    public LocalDate getDate() { return date; }
    public void setDate(LocalDate date) { this.date = date; }
}
```

  2. 用于存储和管理新闻的 CDI bean
```java
import jakarta.enterprise.context.ApplicationScoped;
import jakarta.inject.Named;
import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.atomic.AtomicLong;

@Named
@ApplicationScoped
public class NewsService {
    private final List<News> newsList = new ArrayList<>();
    private final AtomicLong idGenerator = new AtomicLong(1);

    // Получить все новости (динамический список)
    public List<News> getAllNews() {
        return newsList;
    }

    // Добавить новую новость
    public void addNews(News news) {
        news.setId(idGenerator.getAndIncrement());
        newsList.add(0, news); // Добавляем в начало ленты
    }
}
```

  3. 表单的托管 bean
```java
import jakarta.enterprise.context.RequestScoped;
import jakarta.inject.Inject;
import jakarta.inject.Named;

@Named
@RequestScoped
public class NewsBean {
    @Inject
    private NewsService newsService;

    private String title;
    private String description;
    private String imageUrl;

    // Метод для создания новости
    public String createNews() {
        News news = new News(title, description, imageUrl);
        newsService.addNews(news);
        // Очистка формы
        title = null;
        description = null;
        imageUrl = null;
        return "news?faces-redirect=true"; // Переход обратно на страницу
    }

    // Геттеры и сеттеры
    public String getTitle() { return title; }
    public void setTitle(String title) { this.title = title; }

    public String getDescription() { return description; }
    public void setDescription(String description) { this.description = description; }

    public String getImageUrl() { return imageUrl; }
    public void setImageUrl(String imageUrl) { this.imageUrl = imageUrl; }
}
```

  4. 页面html
```html
<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:h="jakarta.faces.html"
      xmlns:f="jakarta.faces.core">
<h:head>
    <title>Лента новостей</title>
</h:head>
<h:body>
    <h1>Лента новостей</h1>

    <!-- Форма создания новости -->
    <h:form>
        <h:panelGrid columns="2">
            <h:outputLabel value="Заголовок:" for="title"/>
            <h:inputText id="title" value="#{newsBean.title}" required="true"/>

            <h:outputLabel value="Описание:" for="description"/>
            <h:inputTextarea id="description" value="#{newsBean.description}" required="true"/>

            <h:outputLabel value="URL изображения:" for="imageUrl"/>
            <h:inputText id="imageUrl" value="#{newsBean.imageUrl}" required="true"/>

            <h:commandButton value="Добавить новость" action="#{newsBean.createNews}"/>
        </h:panelGrid>
    </h:form>

    <hr/>

    <!-- Динамический список новостей из CDI бина -->
    <h:dataTable value="#{newsService.allNews}" var="news">
        <h:column>
            <f:facet name="header">Новость</f:facet>
            <h:panelGrid columns="1">
                <h:outputText value="#{news.title}" style="font-weight: bold; font-size: 1.2em"/>
                <h:graphicImage url="#{news.imageUrl}" width="300"/>
                <h:outputText value="#{news.description}"/>
                <h:outputText value="#{news.date}" style="font-style: italic; color: gray"/>
            </h:panelGrid>
        </h:column>
    </h:dataTable>
</h:body>
</html>
```