前端 (JSP + JavaScript) 
    ↓
控制器 (ControllerServlet) 
    ↓
业务逻辑 (AreaCheckServlet)
    ↓
数据模型 (HitResult)

Инициализировать контекст приложения  
初始化应用上下文  
Предотвращение проблем одновременной инициализации  
防止并发初始化问题  

Единый вход → Пересылка в AreaCheckServlet (/area) для обработки  
Множественный вход → Обработка непосредственно в ControllerServlet (избегайте множественных пересылок)  
Очистка запроса → Очистка глобальной истории  

单点提交 → 转发到 AreaCheckServlet（/area）处理  
多点提交 → 在 ControllerServlet 中直接处理（避免多次转发）  
清除请求 → 清空全局历史记录  

ControllerServlet.java - 充当前端控制器  
Единая точка входа: все запросы сначала попадают сюда, что предотвращает прямой доступ клиентов к AreaCheckServlet.  
统一入口：所有请求先到这里，避免客户端直接访问 AreaCheckServlet  
